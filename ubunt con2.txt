lshw

apt list | grep isomaster

sudo apt install engrampa

sudo apt-get install indicator-applet-complete

echo -n "rastgoo" | sha256sum

sudo dpkg-repack vlc 
 
sudo nano /etc/proxychains4.conf


nautilus

conky -c ./themsrc

sudo apt-get install network-manager-sstp  sstp-client

sudo apt install stacer

systemd-analyze


delete folder 
sudo rm -r folder name


add repo
sudo add-apt-repository ppa:adress

نصب فونت مایکرو سافت
sudo apt-get install ttf-mscorefonts-installer

sudo fc-cache -f -v

sudo apt install uget

sudo apt install unace unrar zip unzip p7zip-full p7zip-rar sharutils rar uudeview mpack arj cabextract file-roller



 && sudo pm-hibernate
sudo nano /var/lib/polkit-1/localauthority/10-vendor.d/com.ubuntu.desktop.pkla
با دستور فوق فایلی داخل ترمینال باز خواهد شد. به سمت پایین اسکرول کنید و دنبال دو قسمت با نام های“[Disable hibernate by default in upower]”“[Disable hibernate by default in logind]” بگردید.
هر دو قسمتی که بالا اسمش رو آوردم دارای مقداری به نامResultActiveهستند، که د yes شوند. سپسCtrl+X را بزنید و بعدشEnter و تمام.

 لیست کردن بسته های نصب شده
sudo apt list --installed

snap list

sudo snap remove app

sudo dpkg --purge rox

sudo aptitude purge roxfiler
sudo apt autoclean program
sudo apt --purge remove program

------

$ sudo apt-get install cups

$ sudo /etc/init.d/cups start

$ sudo /etc/init.d/cups restart

$ sudo /etc/init.d/cups status

$ sudo /etc/init.d/cups stop
در صورتی هم که بخواهیم نرم‌افزار در حین بوت شدن سیستم استارت شو



$ sudo update-rc.d cups enable


$ sudo update-rc.d cups disable

-------


sudo killall apt apt-get

برای مجدد کانفیگ کردن پکیج ها

sudo rm /var/lib/apt/lists/lock
sudo rm /var/cache/apt/archives/lock
sudo rm /var/lib/dpkg/lock

sudo dpkg --configure -a
sudo apt install -f

sudo apt-get install --fix-broken && sudo apt-get autoremove && sudo apt-get update




برای حذف بسته های دانلود شده

sudo apt autoclean

sudo apt list --installed


-----

sudo kill -5 -1

xkill

sudo dpkg-reconfigure linux-sound-base

-----

sudo sh VMware-Player.bundle
----
FOR MAC

sudo apt install firmware-b43-installer

sudo apt install linux-firmware

-------
 sudo dpkg-repack vlc 
 
 ./configure
make
sudo make install

 rdfind


